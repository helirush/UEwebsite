How to read the M P T S savings view: first orient to the harmonized chart, next compare before-and-after field behavior, then focus on reliability and recovery signals before planning rollout. **PL1M — Reactive and MPTS Outcome Notes (Plain Language for Clerk)** For transformer PL1M, the reactive compensation picture is not yet available. The kVAR MAX value needed to describe the reactive burden — and the gap between the current tuned state and the optimization target — returned zero from the data export. No image showing the reactive load profile is available either. When you present this to the facility manager, frame it this way: the three-view optimization arc we use for a tuned field — simulated historical baseline, current tuned baseline, and optimization target — is fully set up for PL1M. We're ready to show them exactly what their installed MPTS equipment has already achieved in reactive reduction and heat burden relief, and exactly how much further the field can go. We just need the actual PL1M readings to make those comparisons. The savings and heat burden recovery story for this transformer is ready to be told. The data is the missing piece, and once it's submitted, we can immediately calculate the achieved reactive reduction from SB to TB, the residual reactive gap from TB to TBT, and the corresponding thermal burden and cooling cost implications — all in concrete, specific numbers.