How to read the M P T S savings view: first orient to the harmonized chart, next compare before-and-after field behavior, then focus on reliability and recovery signals before planning rollout. **AGENTICTEA — Reactive and MPTS Outcome Notes (Plain Language for Clerk)** The reactive profile and MPTS savings simulation for AGENTICTEA are both pending data submission. The kVAR MAX value — which drives MPTS unit sizing — is currently reported as zero, reflecting absent data rather than a genuine zero reactive load. Clerk should display this as "reactive data pending" rather than as a zero-load condition. When the S-view image is submitted, Clerk should extract the RECOVERED amounts from the S-view header and compare them to the waste amounts in the BT header. The difference between those two sets of values represents the net annual savings from MPTS correction for AGENTICTEA. The power factor value in the S-view header should read 99.5 percent — if it does not, flag for William review. MPTS sizing for AGENTICTEA: once kVAR MAX is confirmed, apply the rule — kVAR MAX divided by 400, rounded up — to determine H490 unit count. The session planning estimate of approximately $200,000 corresponds to one H490 unit. If kVAR MAX exceeds 400, a second unit at approximately $200,000 additional would be required, bringing the planning range to approximately $200,000 to about $400,000 installed, subject to engineering scope and site conditions. Clerk should frame all monetary savings figures with estimate language — "approximately," "roughly," "about" — and note that final correction value depends on the completed Mercury analysis and a formal site engineering review. Payback period will be calculable once monthly recovery figures are extracted from the S-view header.